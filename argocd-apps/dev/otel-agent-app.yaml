apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: dev-otel-agent
  namespace: argocd
spec:
  project: default
  destination:
    server: https://kubernetes.default.svc
    namespace: monitoring-dev
  sources:
    - repoURL: https://github.com/open-telemetry/opentelemetry-helm-charts/releases/download/opentelemetry-collector-0.157.2
      chart: open-telemetry-collector
      targetRevision: 0.157.2
      helm:
        releaseName: dev-otel
        valueFiles:
          - $gitea-values/apps/opentelemetry/agent/base-agent.yaml
          - $gitea-values/apps/opentelemetry/agent/dev-values.yaml
    - repoURL: https://github.com/mcheng-splunk/k8s-infrastructure-gitops.git
      targetRevision: main
      ref: gitea-values
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true